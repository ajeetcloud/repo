each node will return whetehr it found p or q or null, so every node will return TreeNode.
leftAnswer = dfs(node.left)
rightAnswer = dfs(mode.right);

if ist not found it will be null.

when leftAnswer != null and leftAnswer == p and rightAnswer == q
return currentNode as final answer.

rough solution
